{
    "model": {
        "backbone": {
            "type": "resnet",
            "out_channels": 256,
            "pretrained": true
        },
        "num_classes": 3,
        "min_size": 800,
        "max_size": 1333
    },
    "data": {
        "train_dir": "data/train",
        "val_dir": "data/val",
        "test_dir": "data/test"
    },
    "training": {
        "batch_size": 4,
        "num_epochs": 100,
        "learning_rate": 0.001,
        "num_workers": 4,
        "save_dir": "checkpoints/rcnn"
    }
}